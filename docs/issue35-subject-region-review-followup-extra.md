# Issue #35 — Subject-region acceptance rule

For the current acceptance run, validated `shot_regions` evidence is the only authority for populating `persian.shots[].avoidRegions`. The resulting recovery child may change only the `subject_regions` scope; copy, assets, scenes, typography, watermark, audio, hook, captions, and timeline remain frozen.
